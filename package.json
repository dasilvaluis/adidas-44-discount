{
  "name": "adidas-44-discount-plugin",
  "version": "1.0.0",
  "description": "Chrome extension that shows adidas products with 44% discount",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "NODE_ENV=production webpack --config webpack.config.js",
    "watch": "NODE_ENV=development webpack --config webpack.config.js -w",
    "clean": "rm -rf dist && cpy manifest.json dist && cpy src/assets/* dist/assets",
    "prebuild": "npm run clean",
    "prewatch": "npm run clean"
  },
  "dependencies": {
    "webpack": "^5.36.2",
    "webpack-cli": "^4.7.0"
  },
  "devDependencies": {
    "cpy-cli": "^3.1.1"
  }
}
